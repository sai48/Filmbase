%h1 Listing add_users_to_films

%table
  %tr
    %th Users
    %th Film
    %th
    %th
    %th

  - @add_users_to_films.each do |add_users_to_film|
    %tr
      %td= add_users_to_film.users_id
      %td= add_users_to_film.film_id
      %td= link_to 'Show', add_users_to_film
      %td= link_to 'Edit', edit_add_users_to_film_path(add_users_to_film)
      %td= link_to 'Destroy', add_users_to_film, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Add users to film', new_add_users_to_film_path
